<% if @room.valid? %>
$('#mainModal').modal('hide').on('hidden.bs.modal', function (e) {
    Turbolinks.visit(window.location);
});
<% else %>
$('#form_new_room').html('<%=j render template: 'admin/room_management/new' %>')
<% end %>